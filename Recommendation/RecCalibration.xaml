<Page
    x:Class="ReviewR.RecCalibration"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ReviewR"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="canvasgrid" Background="#D55E5E">
        <Grid x:Name="CanvasGrid">
            <Grid.RowDefinitions>
                <RowDefinition Height="250"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="350"/>
                <ColumnDefinition Width="1220"/>
                <ColumnDefinition Width="350"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Row="0" Grid.ColumnSpan="3" Background="Black">
                <TextBlock x:Name="recommendationcalibration_text" Grid.Row="0" Text="Recommendation Calibration" TextWrapping="Wrap" FontSize="75" TextAlignment="Center" FontWeight="Bold" Margin="277,65,276,65"/>
            </Grid>

            <Grid x:Name="StartGrid" Grid.Row="1" Grid.ColumnSpan="3" Background="#D55E5E" Height="830" VerticalAlignment="Top">
                <TextBlock x:Name="calhelp_description" Text="Hey username! As this is your first time here, you will be required to calibrate your preferences through an algorithm before you receive personalised recommendations." TextWrapping="Wrap" FontSize="50" TextAlignment="Center" FontWeight="Bold" Margin="68,194,67,412" Visibility="Collapsed"/>
                <Button x:Name="startrec_button" Content="Begin calibration activity" Width="650" FontSize="50" VerticalAlignment="Stretch" FontWeight="Bold" Margin="0,467,0,222" HorizontalAlignment="Center" Click="startrec_button_Click" Visibility="Collapsed"/>
            </Grid>

            <Grid x:Name="GameGrid" Grid.Row="1" Grid.Column="1">
                <Rectangle Margin="100,100,100,100" Stroke="Black" StrokeThickness="5" Fill="Gray"/>

            </Grid>

            <Grid Grid.Row="1" Grid.Column="0">
                <Button Content="Dislike" FontSize="40" FontWeight="Bold" Background="Gray" BorderThickness="5" BorderBrush="Black" Margin="100,200,0,200" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>

            </Grid>

            <Grid Grid.Row="1" Grid.Column="2">
                <Button Content="Like" FontSize="40" FontWeight="Bold" Background="Gray" BorderThickness="5" BorderBrush="Black" Margin="0,200,100,200" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
            </Grid>
        </Grid>
    </Grid>
</Page>
